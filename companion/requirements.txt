websockets>=12.0
zeroconf>=0.131.0
# Optional: powers the D-Bus service the GNOME extension and KDE applet
# subscribe to. Skipped on macOS, which has no D-Bus session bus.
dbus-fast>=2.0.0; sys_platform != "darwin"
