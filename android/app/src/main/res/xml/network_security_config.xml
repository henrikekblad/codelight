<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- This app connects to ws:// on the local LAN only — cleartext is acceptable -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
